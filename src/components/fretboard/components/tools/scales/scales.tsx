import { Note  as NoteModel } from 'FretboardModels';

interface Props {
    noteModel: NoteModel
}

const Scales = ({noteModel}:Props): JSX.Element => {
    return (
        <div>

        </div>
    )
  }
  
  export {Scales}